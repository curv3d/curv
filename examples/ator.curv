torus {major: 2, minor: 1}
    >> texture (t_radial_rainbow 1)
    >> rotate {angle: tau/4, axis: Y_axis}
    >> twist (3)
    >> lipschitz 2.2

where
include file "lib/experimental.curv";
t_radial_rainbow n =
    (x,y,z,t)->sRGB.hue(phase(x,y)/tau*n+t/8 `mod` 1);
